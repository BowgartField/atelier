[package]
name = "jean-server"
version = "0.1.65"
edition = "2021"

[dependencies]
jean-core = { path = "../crates/jean-core" }
tokio = { version = "1", features = ["macros", "rt-multi-thread", "signal"] }
